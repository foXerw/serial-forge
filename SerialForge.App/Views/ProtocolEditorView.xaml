<Window x:Class="SerialForge.App.Views.ProtocolEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SerialForge.App.ViewModels"
        xmlns:c="clr-namespace:SerialForge.App.Converters"
        xmlns:core="clr-namespace:SerialForge.Core;assembly=SerialForge.Core"
        Title="协议编辑器" Height="760" Width="1040">
    <Window.Resources>
        <c:ByteOrderConverter x:Key="ByteOrder"/>
        <c:VisibilityConverter x:Key="Vis"/>
    </Window.Resources>
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2">
            <TextBlock Text="文件：" VerticalAlignment="Center"/>
            <TextBox Text="{Binding FilePath}" Width="300" IsReadOnly="True" Margin="4,0"/>
            <Button Content="打开…" Command="{Binding Open}" Margin="4,0"/>
            <Button Content="另存为…" Command="{Binding SaveAs}" Margin="4,0"/>
        </StackPanel>

        <TabControl Grid.Row="1" Margin="2,4">
            <TabItem Header="可视化">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="2">
                    <StackPanel>
                        <!-- Header -->
                        <GroupBox Header="协议" Margin="2,4">
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <TextBlock Text="名称：" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding Name}" Width="120" Margin="4,0,12,0"/>
                                <TextBlock Text="版本：" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding Version}" Width="50" Margin="4,0,12,0"/>
                                <TextBlock Text="默认字节序：" VerticalAlignment="Center"/>
                                <ComboBox ItemsSource="{x:Static c:ByteOrderConverter.OptionsFixed}" SelectedItem="{Binding DefaultByteOrder, Converter={StaticResource ByteOrder}}" Width="70" Margin="4,0,12,0"/>
                                <TextBlock Text="帧超时(ms)：" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding FrameTimeoutMs}" Width="50" Margin="4,0"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Frame segments -->
                        <GroupBox Header="帧字段 Frame（顺序即字节顺序）" Margin="2,4">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding Segments}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:SegmentViewModel}">
                                            <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="2">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="↑" Width="22" Margin="1" Padding="0"
                                                                Command="{Binding DataContext.MoveSegmentUp, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                                        <Button Content="↓" Width="22" Margin="1" Padding="0"
                                                                Command="{Binding DataContext.MoveSegmentDown, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                                        <Button Content="✕" Width="22" Margin="1,1,6,1" Padding="0" Foreground="#C00"
                                                                Command="{Binding DataContext.RemoveSegment, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                                        <TextBox Text="{Binding Name}" Width="80" Margin="2" ToolTip="名称"/>
                                                        <TextBlock Text="角色：" VerticalAlignment="Center"/>
                                                        <ComboBox ItemsSource="{x:Static vm:SegmentViewModel.Roles}" SelectedItem="{Binding Role}" Width="74" Margin="2"/>
                                                        <TextBlock Text="位宽：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Width}" Width="40" Margin="2" ToolTip="位宽（留空=变长payload）"/>
                                                        <ComboBox ItemsSource="{x:Static c:ByteOrderConverter.Options}" SelectedItem="{Binding ByteOrder, Converter={StaticResource ByteOrder}}" Width="58" Margin="2" ToolTip="字节序（默认=协议默认）"/>
                                                    </StackPanel>
                                                    <!-- Fixed -->
                                                    <StackPanel Orientation="Horizontal" Margin="24,2,0,2" Visibility="{Binding IsFixed, Converter={StaticResource Vis}}">
                                                        <TextBlock Text="value(hex)：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Value}" Width="120" Margin="2" ToolTip="如 AA 55"/>
                                                    </StackPanel>
                                                    <!-- Value -->
                                                    <StackPanel Orientation="Horizontal" Margin="24,2,0,2" Visibility="{Binding IsValue, Converter={StaticResource Vis}}">
                                                        <TextBlock Text="default：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Default}" Width="60" Margin="2"/>
                                                        <TextBlock Text="enum：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Enum}" Width="140" Margin="2" ToolTip="0:idle,1:run"/>
                                                    </StackPanel>
                                                    <!-- Length -->
                                                    <StackPanel Orientation="Horizontal" Margin="24,2,0,2" Visibility="{Binding IsLength, Converter={StaticResource Vis}}">
                                                        <TextBlock Text="counts：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Counts}" Width="120" Margin="2" ToolTip="计入长度的字段名，逗号分隔"/>
                                                        <TextBlock Text="offset：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Offset}" Width="50" Margin="2"/>
                                                    </StackPanel>
                                                    <!-- Checksum -->
                                                    <StackPanel Orientation="Horizontal" Margin="24,2,0,2" Visibility="{Binding IsChecksum, Converter={StaticResource Vis}}">
                                                        <TextBlock Text="algo：" VerticalAlignment="Center"/>
                                                        <ComboBox ItemsSource="{x:Static vm:SegmentViewModel.Algos}" SelectedItem="{Binding Algo}" Width="60" Margin="2"/>
                                                        <TextBlock Text="from：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding OverFrom}" Width="56" Margin="2"/>
                                                        <TextBlock Text="to：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding OverTo}" Width="56" Margin="2"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="24,2,0,2" Visibility="{Binding IsCrc, Converter={StaticResource Vis}}">
                                                        <TextBlock Text="poly：" VerticalAlignment="Center"/><TextBox Text="{Binding Poly}" Width="56" Margin="1"/>
                                                        <TextBlock Text="init：" VerticalAlignment="Center"/><TextBox Text="{Binding Init}" Width="56" Margin="1"/>
                                                        <TextBlock Text="xorOut：" VerticalAlignment="Center"/><TextBox Text="{Binding XorOut}" Width="56" Margin="1"/>
                                                        <CheckBox Content="refIn" IsChecked="{Binding RefIn}" Margin="2,0"/>
                                                        <CheckBox Content="refOut" IsChecked="{Binding RefOut}" Margin="2,0"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="+ 添加字段" Command="{Binding AddSegment}" Margin="2,4" HorizontalAlignment="Left" Padding="6,2"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Commands -->
                        <GroupBox Header="命令 Commands" Margin="2,4">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding Commands}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:CommandEditorViewModel}">
                                            <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="2" Margin="0,2">
                                                <StackPanel Margin="2">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="✕" Width="22" Margin="1,1,6,1" Padding="0" Foreground="#C00"
                                                                Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                                        <TextBlock Text="name：" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding Name}" Width="110" Margin="4,0"/>
                                                        <TextBlock Text="title：" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                                        <TextBox Text="{Binding Title}" Width="120" Margin="4,0"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="20,2,0,0">
                                                        <TextBlock Text="values：" VerticalAlignment="Center"/>
                                                        <ItemsControl ItemsSource="{Binding Values}">
                                                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type vm:FixEntry}">
                                                                    <StackPanel Orientation="Horizontal" Margin="2,0">
                                                                        <TextBox Text="{Binding Field}" Width="70" Margin="1" ToolTip="字段名"/>
                                                                        <TextBlock Text="=" VerticalAlignment="Center"/>
                                                                        <TextBox Text="{Binding Value}" Width="70" Margin="1"/>
                                                                        <Button Content="✕" Width="20" Margin="1,0" Padding="0" Foreground="#C00"
                                                                                Command="{Binding DataContext.RemoveValue, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <Button Content="+ value" Width="50" Margin="4,0" Padding="2" Command="{Binding AddValue}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="+ 添加命令" Command="{Binding AddCommand}" Margin="2,4" HorizontalAlignment="Left" Padding="6,2"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="原始 JSON">
                <Grid Margin="2">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2">
                        <Button Content="刷新（JSON ← 可视化）" Command="{Binding RefreshRaw}" Margin="2,0"/>
                        <Button Content="应用（可视化 ← JSON）" Command="{Binding ApplyRaw}" Margin="2,0"/>
                        <TextBlock Text="payload 子模板等高级结构在此编辑" VerticalAlignment="Center" Margin="8,0" Foreground="Gray"/>
                    </StackPanel>
                    <TextBox Grid.Row="1" Text="{Binding RawJson, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2" Margin="2,6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding ErrorMessage}" Foreground="#C00" TextWrapping="Wrap" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="取消" Width="80" Margin="4,0" Click="Cancel_Click"/>
            <Button Grid.Column="2" Content="应用" Width="80" Margin="4,0" Command="{Binding Apply}"/>
        </Grid>
    </Grid>
</Window>
